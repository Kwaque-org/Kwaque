--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -20,20 +20,31 @@
         "src/include/liburing/io_uring_version.h",
     ],
     cmd = """
-            export CC=$(CC) CXX=$(CC)++
+            export CC=$(CC) CXX=$(CC)++ CFLAGS="$(CC_FLAGS)"
+            # Configuration probes need compilation only. Avoid invoking a
+            # host linker that cannot resolve hermetic sysroot scripts.
+            export LDFLAGS="-c"
 
-            # switch to the package dir to execute "configure" right there
-            pushd $$(dirname $(location configure))
-              mkdir -p src/include/liburing
-              ./configure --use-libc
-            popd
+            # Keep compiler paths valid by configuring from the output tree.
+            pkg_dir=$$(dirname $(location configure))
+            ln -sf $$pkg_dir/Makefile.common Makefile.common
+            ln -sf $$pkg_dir/liburing.spec liburing.spec
+            mkdir -p src/include/liburing
+            $$pkg_dir/configure --use-libc
 
             # collect the outputs
             for out in $(OUTS); do
-              cp $$(realpath --relative-to=$(BINDIR) $$out) $$out
+              cp $${out#$(RULEDIR)/} $$out
             done
           """,
-    toolchains = ["@bazel_tools//tools/cpp:current_cc_toolchain"],
+    toolchains = [
+        "@bazel_tools//tools/cpp:current_cc_toolchain",
+        "@bazel_tools//tools/cpp:cc_flags",
+    ],
     tools = ["configure"],
 )
@@ -76,6 +87,7 @@
         "-Wall",
         "-Wextra",
         "-fno-stack-protector",
+        "-Wno-unused-parameter",
         "-include config-host.h",
     ],
     includes = ["src/include"],
